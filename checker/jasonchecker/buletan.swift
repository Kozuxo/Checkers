import Foundation
import SwiftUI
struct bulets{
        let id = UUID()
        var hidup: Bool = true
        var rajas: Bool = false
        var color = Color.red
        var dipilih: Bool = false
        var transparan: Double = 0
    }
